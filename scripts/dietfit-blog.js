/* Blog giờ là trang tĩnh do tools/build.py sinh ra từ content/blog-posts.json
   (blog/<slug>/index.html), nên không còn cần render bằng JavaScript.
   File này được giữ lại rỗng để trình duyệt nào còn cache HTML cũ không bị lỗi 404. */
